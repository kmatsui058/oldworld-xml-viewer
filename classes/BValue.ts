export default class BValue {
  readonly key: string
  readonly pair: Readonly<XmlEffectCity.Pair2[]>
  constructor(key: BValue['key'], pair: BValue['pair']) {
    this.key = key
    this.pair = pair
  }
}
