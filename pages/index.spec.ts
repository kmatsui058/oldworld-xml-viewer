import { mount } from '@vue/test-utils'
import Index from '@/pages/index.vue'
describe('index page', () => {
  describe('index', () => {
    test('index page', () => {
      mount(Index)
    })
  })
})
